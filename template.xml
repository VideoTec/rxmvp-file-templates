<template format="4"
        revision="1"
        name="Rx MPV"
        description="Creates a new RxMVP classes - Presenter, Model, Bean and Contract">

    <category value="Other"/>

    <parameter id="className"
        name="Prefix Name"
        type="string"
        constraints="class|unique|nonempty"
        default="MvpFunction"
        help="The prefix of class name"/>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>